import 'package:flutter/material.dart';

class Themes {
  Themes();
  static const Color primaryColor = Color.fromRGBO(167, 112, 239, 1);
  static const Color fontColor = Colors.black87;
  static const Color foregroundColor = Colors.black87;
  static const Color defaultIconColor = Colors.black54;
  static Color? layoutBackgroundLight = Colors.grey[300];
  static Color? onLayoutBackgroundLight = Colors.grey[400];
  static Color? cardColor = Colors.grey[100];

  static const Color iconDisabled = Colors.black26;
  static const Color iconEnabled = primaryColor;
  static Color? errorColor = Colors.red[600];
}
